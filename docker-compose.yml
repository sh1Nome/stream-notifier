services:
  jdk:
    build: .
    init: true
    volumes:
      - './work:/home/work'
    env_file:
      - './.env'
  db:
    image: 'postgres:16'
    volumes:
      - './data:/var/lib/postgresql/data'
    env_file:
      - './.env'
  adminer:
    image: 'adminer'
    ports:
      - '49152:8080'
